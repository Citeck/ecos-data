{
  "id": "ecos-data-messages",
  "locales": [
    "en",
    "ru"
  ],
  "messages": {
    "ecos-data.permission-denied.record": [
      "Permissions Denied. You can't change record {{recordRef}}",
      "Доступ запрещен. Вы не можете изменять запись {{recordRef}}"
    ],
    "ecos-data.permission-denied.attributes": [
      "Permissions Denied. You can't change attributes {{attributes}} for record {{recordRef}}",
      "Доступ запрещен. Вы не можете изменять атрибуты {{attributes}} для записи {{recordRef}}"
    ],
    "ecos-data.permission-denied.user-cannot-change-attribute": [
      "Permission Denied. User {{user}} can't change attribute {{attribute}} for record {{recordRef}}",
      "Доступ запрещен. Пользователь {{user}} не может изменить атрибут {{attribute}} для записи {{recordRef}}"
    ],
    "ecos-data.permission-denied.delete": [
      "Permissions Denied. You can't delete record {{recordRef}}",
      "Доступ запрещен. Вы не можете удалить запись {{recordRef}}"
    ],
    "ecos-data.invalid-type.subtype": [
      "Invalid type '{{typeId}}'. In source '{{sourceId}}', mutations are allowed only for type '{{cfgType}}' and its subtypes.",
      "Некорректный тип '{{typeId}}'. В источнике данных '{{sourceId}}' мутации разрешены только для типа '{{cfgType}}' и его подтипов."
    ],
    "ecos-data.invalid-type.ws-scoped": [
      "Invalid type '{{typeId}}'. In source '{{sourceId}}', mutations with workspace-scoped types are not allowed.",
      "Некорректный тип '{{typeId}}'. В источнике данных '{{sourceId}}' мутации с workspace-scoped типами запрещены."
    ],
    "ecos-data.type-not-found-by-id": [
      "Type with id '{{typeId}}' was not found.",
      "Тип с идентификатором '{{typeId}}' не найден."
    ],
    "ecos-data.has-non-unique-attributes": [
      "{{recordRef}} has non-unique attributes {{attributes}}",
      "{{recordRef}} имеет не уникальные значения атрибутов {{attributes}}"
    ]
  }
}

