pipelines:
    branches:
      develop:
        - step:
           script:
             - git remote add sync git@github.com:Citeck/ecos-data.git
             - git checkout develop
             - git pull
             - git push sync develop